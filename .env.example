# Copy this file to .env.local and paste your Anthropic API key.
# The assistant chatbot ("Doss AI") uses this to call the real Claude model.
# Without it, the app still works: the chatbot falls back to the local engine.
#
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
